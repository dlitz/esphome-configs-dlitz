sensor:
  - platform: wifi_signal # Reports the WiFi signal strength/RSSI in dB
    name: "WiFi Signal dB"
    id: wifi_signal_db
    update_interval: 60s

  - platform: copy # Reports the WiFi signal strength in %
    id: wifi_signal_percent
    source_id: wifi_signal_db
    name: "WiFi Signal Percent"
    filters:
      - lambda: return min(max(2 * (x + 100.0), 0.0), 100.0);
    unit_of_measurement: "Signal %"
    entity_category: diagnostic
    device_class: ""

text_sensor:
  - id: wifi_info_text
    platform: wifi_info
    ip_address:
      id: ip_address_text
      name: Device IP Address
      address_0:
        id: ip_address_0_text
        name: Device IP Address 0
      address_1:
        id: ip_address_1_text
        name: Device IP Address 1
      address_2:
        id: ip_address_2_text
        name: Device IP Address 2
      # address_3:
      #   id: ip_address_3_text
      #   name: Device IP Address 3
      # address_4:
      #   id: ip_address_3_text
      #   name: Device IP Address 4
    ssid:
      id: ssid_text
      name: Device Connected SSID
    bssid:
      id: bssid_text
      name: Device Connected BSSID
    mac_address:
      id: mac_address_text
      name: Device MAC Address
    # scan_results:
    #   id: scan_results_text
    #   name: Device Latest Scan Results
    # dns_address:
    #   id: dns_address_text
    #   name: Device DNS Address
    # power_save_mode:
    #   id: power_save_mode_text
    #   name: Device Wifi Power Save Mode
